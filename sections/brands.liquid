{% comment %}
  Brands (Varumärken) section.
  Renders all unique product vendors server-side via shop.vendors (sorted
  alphabetically by Shopify), grouped under letter headings with links to the
  built-in vendor collection. Replaces an earlier client-side version that
  crawled /products.json (20+ requests on a 5000+ product store) and left the
  page on "Laddar varumärken…" for a long time.
{% endcomment %}

<div class="section page-width-content">
  <div class="brands-page" id="brands-list">
  {%- liquid
    assign letters = 'ABCDEFGHIJKLMNOPQRSTUVWXYZÅÄÖ'
    assign prev_key = nil
  -%}
  {%- for vendor in shop.vendors -%}
    {%- liquid
      assign first = vendor | slice: 0, 1 | upcase
      if letters contains first
        assign key = first
      else
        assign key = '1'
      endif
    -%}
    {%- if key != prev_key -%}
      {%- unless forloop.first -%}
        </ul></div>
      {%- endunless -%}
      <div class="brands-group">
        <h2 class="brands-group__letter">{{ key }}</h2>
        <ul class="brands-group__list">
    {%- endif -%}
    <li><a href="{{ vendor | url_for_vendor }}">{{ vendor }}</a></li>
    {%- assign prev_key = key -%}
    {%- if forloop.last -%}
      </ul></div>
    {%- endif -%}
  {%- endfor -%}
  </div>
</div>

{% style %}
  .brands-page {
    padding-block-start: {{ section.settings.padding-block-start }}px;
    padding-block-end: {{ section.settings.padding-block-end }}px;
  }

  /* Widen the list beyond the theme's content column so the empty flanking
     columns are half as wide: new width = (column + viewport) / 2, centered
     via the negative margins. */
  @media screen and (min-width: 990px) {
    .brands-page {
      width: calc(50% + 50vw);
      margin-inline: calc((100% - 100vw) / 4);
    }
  }

  /* Letter groups arranged in a 3-column grid, matching the live site layout */
  #brands-list {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem 3rem;
    align-items: start;
  }

  @media screen and (max-width: 749px) {
    #brands-list {
      grid-template-columns: repeat(2, 1fr);
      gap: 1.5rem 2rem;
    }
  }

  /* Letter headings: one title size below the page title — the theme's h2
     preset (fluid clamp, scales down on mobile by itself); weight follows the
     site-wide preset. */
  .brands-group__letter {
    font-size: var(--font-h2--size, 2.5rem);
    font-weight: var(--font-h2--weight, 400);
    line-height: 1;
    margin-top: 0;
    margin-bottom: 1rem;
    padding-bottom: 0;
    letter-spacing: normal;
  }

  .brands-group__list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .brands-group__list li {
    padding-block: 0.15rem;
    line-height: 1.5;
  }

  /* Vendor links: theme link style — underlined by default, underline lifts
     on hover (matches .link), pressed state dims. */
  .brands-group__list a {
    color: var(--color-foreground);
    text-decoration: underline;
    text-decoration-thickness: 0.075em;
    text-underline-offset: 0.125em;
    text-decoration-color: currentColor;
    transition: text-decoration-color var(--animation-speed-medium, 150ms) ease,
      opacity var(--animation-speed-medium, 150ms) ease;
  }

  .brands-group__list a:hover {
    text-decoration-color: transparent;
  }

  .brands-group__list a:active {
    opacity: 0.6;
  }
{% endstyle %}

{% schema %}
{
  "name": "Varumärken",
  "enabled_on": {
    "templates": ["page"]
  },
  "settings": [
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "t:settings.top_padding",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "t:settings.bottom_padding",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 80
    }
  ],
  "presets": []
}
{% endschema %}
